/**
 * Historical cutoff used by the price-path lead-time drift page. The live
 * trading path itself does not use this window.
 */
export const PRICE_PATH_ANALYSIS_END_EXCLUSIVE_MS = Date.UTC(2026, 3, 1);
